# AgentCore Templates

This directory contains ready-to-use templates and skeletons for building new agents using the AgentFactory, decorator-based callbacks, and config-driven setup.

## Contents
- `agent_skeleton.py`: Minimal agent skeleton with docstrings and usage notes
- `agent_config.json`: Example config for a new agent

## Usage
Copy and adapt these files to quickly start a new agent implementation.

# OpenRide CLI (`openride`)

The full usage reference for this CLI now lives at **[`docs/openride_cli.md`](../docs/openride_cli.md)**.

Quick start: `bash scripts/openride.sh <group> ...` (groups: `run`, `scenario`, `analyze`,
`runs`, `services`, `solver`). See the doc for everything.
